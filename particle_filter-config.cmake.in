get_filename_component(PARTICLE_FILTER_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

# place instructions for finding dependencies of this repo here for example:

find_dependency(fmt REQUIRED)
find_dependency(Eigen REQUIRED)

if(NOT TARGET pf::particle_filter)
    include("${PARTICLE_FILTER_CMAKE_DIR}/particle_filter-targets.cmake")
endif()

set(PARTICLE_FILTER_LIBRARIES pf::particle_filter)